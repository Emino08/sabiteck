╔══════════════════════════════════════════════════════════════════╗
║                                                                  ║
║              COPY THIS SQL TO PHPMYADMIN                         ║
║                                                                  ║
╚══════════════════════════════════════════════════════════════════╝

INSTRUCTIONS:
1. Open phpMyAdmin: http://localhost/phpmyadmin
2. Select database: devco_db
3. Click "SQL" tab
4. Copy the SQL below (all of it)
5. Paste into the SQL text area
6. Click "Go" button

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                         COPY FROM HERE ↓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

-- Add phone column to team table
ALTER TABLE team 
ADD COLUMN phone VARCHAR(50) DEFAULT NULL COMMENT 'Contact phone number' 
AFTER email;

-- Add location column to team table
ALTER TABLE team 
ADD COLUMN location VARCHAR(255) DEFAULT NULL COMMENT 'Geographic location or office' 
AFTER phone;

-- Verify the columns were added
DESCRIBE team;

-- Show success message
SELECT 'Phone and location columns added successfully!' AS Status;

-- Show sample data with new columns
SELECT id, name, email, phone, location FROM team LIMIT 5;

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                         COPY UNTIL HERE ↑
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHAT YOU SHOULD SEE AFTER RUNNING:

✅ Query OK, 0 rows affected (for both ALTER TABLE commands)
✅ Table structure showing phone and location columns
✅ Success message: "Phone and location columns added successfully!"
✅ List of team members (with NULL for phone/location initially)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

IF YOU GET AN ERROR:

Error: "Duplicate column name 'phone'"
→ This means the column already exists
→ This is OKAY! The column is already there
→ Skip to verification step below

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERIFICATION (Run this to check if columns exist):

SELECT 
    COLUMN_NAME,
    DATA_TYPE,
    CHARACTER_MAXIMUM_LENGTH
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA = 'devco_db'
  AND TABLE_NAME = 'team'
  AND COLUMN_NAME IN ('email', 'phone', 'location')
ORDER BY ORDINAL_POSITION;

Expected output:
email     | varchar | 255
phone     | varchar | 50
location  | varchar | 255

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

AFTER MIGRATION - UPDATE EXISTING DATA (Optional):

-- Update team member ID 1
UPDATE team 
SET 
    phone = '+232 78 618435',
    location = 'Sierra Leone'
WHERE id = 1;

-- Update team member ID 2
UPDATE team 
SET 
    phone = '+44 20 7946 0958',
    location = 'London, UK'
WHERE id = 2;

-- Verify updates
SELECT id, name, phone, location FROM team;

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

NEXT STEPS:

1. ✅ Columns added to database
2. ✅ Backend already supports phone/location
3. ✅ Frontend already displays phone/location
4. → Update existing team members via admin panel
5. → Add new team members with phone/location
6. → View on public page: http://localhost:5173/team

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DONE! 🎉

Your team table now has phone and location columns!
